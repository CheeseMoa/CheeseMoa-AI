# HTTP 스택(fastapi·starlette·uvicorn 등)은 두지 않는다 — 이 서버는 SQS 폴링 워커로 HTTP 미제공
# (feature-spec §3). 학습용 샘플 healthcare_api.py의 의존성은 의도적으로 미포함 (파일 상단 주석 참고).
annotated-types==0.7.0
click==8.4.1
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
typing-inspection==0.4.2
typing_extensions==4.15.0

# --- AI worker: face pipeline (CHMO) ---
opencv-python-headless>=4.8.0
numpy>=1.26,<2.0
huggingface_hub>=0.23
onnxruntime>=1.26,<2.0
# 눈감음 blendshape 하이브리드(ADR 021)의 TFLite 런타임 — mediapipe pip은 linux aarch64 휠이 없어
# EC2(t4g) 배포 불가, litert는 cp312 manylinux aarch64 휠 제공. face_landmarker.task 내부 모델 실행용.
ai-edge-litert>=2.1,<3
boto3>=1.34
pydantic-settings>=2.6,<3
